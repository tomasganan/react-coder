// Ejemplo 3: Desestructuración

// La destructuración permite extraer valores de arreglos o objetos y asignarlos a variables.

const numbers = [1, 2, 3];
const [a, b, c] = numbers;

console.log(a, b, c); // 1 2 3